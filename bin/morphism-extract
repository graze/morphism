#!/usr/bin/env php
<?php
require 'vendor/autoload.php';

try {
    $cmd = new Graze\Morphism\Command\Extract;
    $cmd->argv($argv);
    $cmd->run();
}
catch(\Exception $e) {
    fprintf(STDERR, "%s\n", $e->getMessage());
    exit(1);
}

